# Build outputs
/build/
/dist/
/dist-pages/

# Copy created by `make serve` so wsv can resolve example imports of
# `../mrbgem/...` from within the served root.
/mrbgem

# direnv local override
.envrc.local

# Editor / OS
.DS_Store
*.swp
*~

# Test artifacts
/node_modules/

# Local mrbgem.rake.lock generated during builds
build_config/*.lock

# Ruby gem build artifacts (cli/)
/cli/*.gem
/cli/.bundle/
/cli/Gemfile.lock
/cli/pkg/

# Misc
*.tsbuildinfo
