# Lilac::Extras — small directive plug-ins demonstrating the
# Scanner extension API. Each directive lives in its own file:
#
#   - lilac_extras_tooltip.rb  → data-tooltip="@msg"
#   - lilac_extras_focus.rb    → data-focus="@signal" + data-autofocus
#
# These also serve as the worked examples for the directive plug-in
# protocol (see docs/lilac-directive-plugin-spec.md).

module Lilac
  module Extras
  end
end
